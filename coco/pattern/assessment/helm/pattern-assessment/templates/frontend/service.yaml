apiVersion: v1
kind: Service
metadata:
  name: {{ include "pattern-assessment.frontend.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pattern-assessment.frontend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "pattern-assessment.frontend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
