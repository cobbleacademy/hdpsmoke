{
  "items": [
    { "category": "Core Port", "item": "Role-based resource security via Spring Security (externalized hsm.security.access-rules)", "status": "C", "notes": "Replaces per-controller scope checks; 26/26 tests passing" },
    { "category": "Core Port", "item": "Maven multi-module scaffold (parent + hsm-core-service + cek-rotation-service)", "status": "C", "notes": "" },
    { "category": "Core Port", "item": "Config layer (HsmProperties, application.yml / application-demo.yml, env-var name parity with Python .env files)", "status": "C", "notes": "" },
    { "category": "Core Port", "item": "Flyway migrations (V1 initial schema, V2 fingerprint column, V3 demo consumer accounts, V4 dev status items)", "status": "C", "notes": "Portable across Postgres + H2" },
    { "category": "Core Port", "item": "Crypto core — DekManager, IvFactory, AES-256-GCM via Bouncy Castle, token pack/unpack", "status": "C", "notes": "9 unit tests passing" },
    { "category": "Core Port", "item": "JPA entities + repositories (EdekRecord, AppRegistration, AppDecryptGrant, ConsumerAccount, DevStatusItem)", "status": "C", "notes": "" },
    { "category": "Core Port", "item": "KEK client (Mock + Azure Key Vault) and DEK cache (Null + Redis)", "status": "C", "notes": "Azure/Redis paths not live-tested — see Backlog" },
    { "category": "Core Port", "item": "Auth — JWT validator (RSA + Mock), AppRegistryService, PBAC client (PlainID + Null)", "status": "C", "notes": "" },
    { "category": "Core Port", "item": "Audit logging — AuditLogger, SplunkHecBatcher, RecentEventsBuffer", "status": "C", "notes": "Batching + retry-requeue covered by tests" },
    { "category": "Core Port", "item": "Request/response DTOs with validation", "status": "C", "notes": "" },
    { "category": "Core Port", "item": "Services — EncryptionService, DecryptionService, RotationService", "status": "C", "notes": "Fixed a KEK-rotation pagination bug found in the Python source" },
    { "category": "Core Port", "item": "Web layer — controllers, exception handling, static UI mount", "status": "C", "notes": "" },
    { "category": "Core Port", "item": "Demo mode wiring + ported static UI assets", "status": "C", "notes": "" },
    { "category": "Core Port", "item": "KEK rotation scheduler", "status": "C", "notes": "" },
    { "category": "Core Port", "item": "cek-rotation-service module (alpha/beta CEK rotation worker)", "status": "C", "notes": "" },
    { "category": "Core Port", "item": "Unit + integration test suite", "status": "C", "notes": "26 tests passing across both modules" },
    { "category": "Core Port", "item": "Dockerfiles for both modules", "status": "C", "notes": "" },
    { "category": "Core Port", "item": "End-to-end demo verification in browser", "status": "C", "notes": "" },

    { "category": "Backlog", "item": "CMVP / FIPS 140-3 compliant Bouncy Castle (swap bcprov-jdk18on → bc-fips)", "status": "C", "notes": "cert #4943, Level 1; approved-mode DRBG wired via CryptoServicesRegistrar" },
    { "category": "Backlog", "item": "Live-test Azure Key Vault KEK wrap/unwrap against a real HSM", "status": "N", "notes": "" },
    { "category": "Backlog", "item": "Live-test PlainID PBAC integration", "status": "N", "notes": "" },
    { "category": "Backlog", "item": "Live-test Redis DEK cache + CEK hot-reload against a real Redis", "status": "N", "notes": "" },
    { "category": "Backlog", "item": "Live-test against a real Postgres instance (multi-schema config)", "status": "N", "notes": "Only demo-mode H2 verified so far" },
    { "category": "Backlog", "item": "Update Helm charts to reference the Java images", "status": "C", "notes": "Java-only images; Python retained in repo but not deployed" },
    { "category": "Backlog", "item": "Convert Architecture / Sequence diagrams to a maintainable format", "status": "N", "notes": "Currently hand-authored static SVG, copied as-is from the Python UI; Mermaid proposed" },
    { "category": "Backlog", "item": "Correlate resource paths with Entra ID App Roles (currently DB-driven only)", "status": "N", "notes": "Documented in java/docs/AUTHORIZATION.md. AppRegistryService's app_registrations DB table is the sole source of authorization today; Entra ID's appid claim is used for identity only. Recommended mechanism is App Roles, not Security Groups (avoids the 200-group claim overage / extra Graph call)." },
    { "category": "Backlog", "item": "Build break-glass recovery tool for total app-lockout scenarios", "status": "N", "notes": "Documented in java/docs/RUNBOOK.md -- no path exists today around a broken JWKS/issuer config or a wiped/corrupted app_registrations table; every recovery path currently requires the same control plane that's broken. Needs security sign-off on scope (dual-control, out-of-band from this service's own API) before building." },
    { "category": "Backlog", "item": "Execute DR restore drills (Managed HSM backup, Postgres point-in-time restore)", "status": "N", "notes": "Documented in java/docs/DISASTER_RECOVERY.md with the actual az CLI commands and a drill checklist. Backup mechanisms exist (Azure-native); untested restore is the real gap -- confirm retention windows and time an actual restore into a scratch environment." },
    { "category": "Backlog", "item": "Improve admin-ops tooling for grants/app-status (CLI wrapper or ticketing integration)", "status": "N", "notes": "Documented in java/docs/ADMIN_OPERATIONS.md. Currently hand-crafted curl calls with an ops-admin token, no approval workflow for cross-app grant changes. Also no endpoint to create a new app_registration at all -- onboarding goes through a versioned migration by design, see java/docs/APP_ONBOARDING.md." },
    { "category": "Backlog", "item": "Performance testing and capture metrics", "status": "N", "notes": "No load/throughput numbers exist yet for encrypt/decrypt latency, HSM unwrap call volume under concurrency, or DEK cache hit ratio under the hot-key vs. bulk patterns described in java/docs/CACHING_AND_ROTATION.md. Related to the concurrency/load test port (in progress) but distinct -- that's correctness under concurrency, this is measured throughput/latency numbers plus ongoing metrics capture (e.g. Micrometer/Actuator + a dashboard), not just pass/fail tests." },
    { "category": "Backlog", "item": "Batch encrypt + decrypt for app on/de-boarding: Tier 1 endpoints", "status": "C", "notes": "POST /encrypt/batch and POST /decrypt/batch both implemented -- multiple items per call, correlated back by a caller-supplied key, always 200 with per-item success/error status. Both reuse the existing single-item encrypt()/decrypt() methods unmodified per item, sequential not concurrent (pending real HSM throughput numbers), share hsm.service.batch-max-items (default 100). Decrypt-batch's token-or-legacy-fields check is a per-item runtime outcome (inside decrypt()), not a whole-batch Bean Validation rejection like encrypt's plaintext checks -- one malformed item doesn't sink the batch. 15 tests total (7 encrypt + 8 decrypt), full suite 55/55. See java/docs/BULK_OPERATIONS.md. Not built: audit duration_ms/time-taken (only counts + a single completion timestamp exist today)." },
    { "category": "Backlog", "item": "Add duration_ms (time taken) to batch_encrypt/batch_decrypt audit events", "status": "N", "notes": "Both audit events currently carry item/success/failure counts and the standard single completion timestamp every audit event gets -- no start time or duration field, so elapsed time can't be derived from the audit trail today. Small: capture Instant.now() at the top of encryptBatch/decryptBatch, compute elapsed millis before the existing audit call, add one field to each. Documented in java/docs/BULK_OPERATIONS.md." },
    { "category": "Backlog", "item": "Publish client-side bulk/chunking reference guidance (not a service)", "status": "N", "notes": "Corrected from an earlier design: bulk/file-chunking is NOT a service this repo builds -- reaching into an app's own DB/blob storage would need a new cross-app storage grant, and bulk crypto-shred already needs zero new capability (deleting ciphertext_token is already effective shredding per DEMO.md Sec6). Instead: document the chunk-per-DEK + manifest + stitch-back pattern for app teams driving their own bulk jobs against the Tier 1 batch endpoint, and consider publishing a thin reference client library. See java/docs/BULK_OPERATIONS.md." },
    { "category": "Backlog", "item": "Tier 3 (proposed): local envelope encryption via a separate hsm-bulk-service + CLNT library", "status": "N", "notes": "Design proposal only, not built, not reviewed -- needs security/crypto sign-off first (Phase 0 of the development plan). App-side CLNT does local AES-256-GCM using DEKs issued by a new, separately deployable hsm-bulk-service (POST /dek/issue, POST /dek/unwrap; new dek_issue/dek_unwrap scopes), each DEK still individually KEK-wrapped and persisted via the HSM exactly as today -- DEK-per-record is preserved, not traded away for throughput. Motivation is reducing SVC's own compute/bandwidth footprint as a shared multi-tenant resource during large onboarding/de-boarding migrations, not reducing HSM RPS (which stays unchanged either way). Scoped strictly to onboarding/de-boarding bookend events, not steady-state traffic -- apps go back to single/Tier-1-batch afterward. See java/docs/BULK_OPERATIONS.md \"Tier 3\" and \"Development plan\" sections." },
    { "category": "Backlog", "item": "Add created_at/updated_at to app_registrations and app_decrypt_grants", "status": "C", "notes": "V5__add_timestamps_to_access_tables.sql. app_registrations gets created_at + updated_at (bumped by setActive/setAllowedScopes); app_decrypt_grants gets created_at only (grants are add/remove, never mutated in place) and it's now exposed via GET/POST /admin/grants (GrantResponse.createdAt). Nullable, no backfill, same pattern as V2's fingerprint column. See java/docs/ADMIN_OPERATIONS.md." },
    { "category": "Backlog", "item": "This development-status tab", "status": "C", "notes": "You're looking at it" },
    { "category": "Backlog", "item": "Development Status tab: edit + persist to DB instead of static JSON", "status": "C", "notes": "DevStatusController (GET/POST/PUT/DELETE), DTOs, seed initializer, and editable UI wired up" },

    { "category": "Security Hardening", "item": "Pin Docker base images by digest (not floating tag)", "status": "C", "notes": "Build stage moved to maven:3.9.9-eclipse-temurin-21 (eclipse-temurin:21-jdk has no mvn binary — pre-existing bug found while pinning); both stages pinned by sha256 digest in both Dockerfiles; verified via full build + container run + live health check" },
    { "category": "Security Hardening", "item": "Add .dockerignore for the java/ build context", "status": "C", "notes": "Excludes target/, .git, IDE files, .env" },
    { "category": "Security Hardening", "item": "Container image vulnerability scan (Trivy) for hsm-core-service", "status": "C", "notes": "Initial scan: 6 critical / 42 high. After remediation below: 0 critical / 0 high (HIGH+CRITICAL scan)." },
    { "category": "Security Hardening", "item": "Container image vulnerability scan (Trivy) for cek-rotation-service", "status": "C", "notes": "0 critical / 0 high — inherits the same patched BOM versions and has a smaller dependency surface (no starter-web/tomcat, no spring-security, no keyvault-keys)." },
    { "category": "Security Hardening", "item": "Remediate CVEs surfaced by image scan", "status": "C", "notes": "spring-boot.version 3.3.5→3.5.16, azure-sdk-bom 1.2.28→1.3.7, plus explicit overrides for io.netty (4.1.136.Final) and org.postgresql:postgresql (42.7.12). Verified: 26/26 tests pass, containers boot and serve/behave correctly." },
    { "category": "Security Hardening", "item": "Distroless/minimal runtime base image", "status": "C", "notes": "eclipse-temurin:21-jre-jammy (full Ubuntu userland) replaced with gcr.io/distroless/java21-debian13:nonroot -- Debian-based, ships Temurin OpenJDK 21 preinstalled, no shell/package manager. Fixed uid/gid 65532 (the distroless nonroot convention) replaces the old groupadd/useradd 'hsmsvc' (1001) user; helm/hsm-core-service/values.yaml securityContext updated to match. Motivated by a real environment issue, not just staleness: behind a corporate firewall, apt-get needed a JFrog-mirrored Ubuntu archive with credentials configured before it could even reach the Jammy repos -- distroless removes apt-get from this Dockerfile entirely, so there's no apt/deb mirror to configure at all anymore." },
    { "category": "Security Hardening", "item": "Remove apt-get from hsm-core-service runtime stage", "status": "C", "notes": "Resolved by the distroless move above -- curl was only ever installed for the Dockerfile HEALTHCHECK, which is now removed. Not a real gap: production health-checking is Kubernetes' own httpGet/tcpSocket livenessProbe/readinessProbe (deployment.yaml), evaluated by the kubelet from outside the container, which never needed curl inside it." },
    { "category": "Security Hardening", "item": "SBOM generation + CI-wired vulnerability scanning", "status": "N", "notes": "No CI pipeline exists in this repo yet to wire scanning into; Trivy scan run manually for now" },
    { "category": "Security Hardening", "item": "Point corporateCa initContainer at a non-distroless utility image", "status": "C", "notes": "Fixed: helm/hsm-core-service/templates/deployment.yaml's import-corporate-ca initContainer now runs against corporateCa.utilityImage (default eclipse-temurin:21-jre-alpine, not a Jammy/Ubuntu variant -- keeps OS-package CVE surface minimal) instead of the app's own distroless image, which has no shell/cp for the `sh -c \"cp ... && keytool ...\"` script. Surfaced by a real K8s JWKS 'certificate_unknown' TLS failure once corporateCa.enabled was turned on -- same corporate TLS-interception issue Python handled via REQUESTS_CA_BUNDLE/SSL_CERT_FILE, which the JVM has no equivalent of; it needs the CA physically imported into a keytool trust store instead. Trust store password also moved from a hardcoded 'changeit' literal to corporateCa.trustStorePassword (still defaults to 'changeit' -- cacerts holds only public certs, so this only guards integrity, not confidentiality -- but no longer a literal string a secret-scanner flags)." },
    { "category": "Bulk (Tier 3) PoC", "item": "hsm-bulk-service (SVC) -- POST /dek/issue + POST /dek/unwrap", "status": "C", "notes": "See java/docs/TIER3_POC_BUILD.md and java/docs/BULK_OPERATIONS.md's Tier 3 section. New module (V6 migration adds app_registrations.public_key_pem), scoped to Phases 1+2 -- admin key-provisioning endpoint (Phase 3) still deferred. Live BulkVsBatchBenchmark (200 records, both services local/mocked): Batch 304 items/sec vs Bulk 145.7 items/sec -- Bulk was slower here, likely RSA-2048 OAEP unwrap overhead per item on the CLNT side; doesn't contradict Tier 3's actual motivation (SVC shared-process footprint under concurrent load, not single-caller throughput), but is a real cost a production pilot needs to size." },
    { "category": "Bulk (Tier 3) PoC", "item": "hsm-bulk-client (CLNT) -- standalone Bulk DB + Bulk File client", "status": "C", "notes": "New module, genuinely separate from SVC (not merged) -- calls POST /dek/issue|unwrap over HTTP, does all AES-GCM locally, no server-side machinery of its own (no auth filter, no JPA). BULK DB: config-driven source/target table+column mapping, independently addressable JDBC URL/schema/table per side, one DEK per column value (not per row), keyset-paginated reads. BULK File: source/target each independently local disk or ADLS (FileStore interface -- mixed pairs like ADLS-to-disk fall out for free), one DEK per whole file, chunks stitched via length-prefixed binary framing (not newline-delimited -- raw AEAD ciphertext can contain a literal 0x0A byte). Verified live end-to-end against real hsm-core-service + hsm-bulk-service: BULK DB 3-row encrypt/decrypt round trip byte-for-byte identical (plus a spot-check through hsm-core-service's real /decrypt); BULK File nested-directory encrypt/decrypt round trip byte-for-byte identical across a multi-chunk (8-chunk) binary file. Answers BULK_OPERATIONS.md's open 'reference client library' question -- this is that client, not a separate future item." },
    { "category": "Bulk (Tier 3) PoC", "item": "hsm-bulk-service Docker/Helm deployment", "status": "C", "notes": "java/docker/Dockerfile.hsm-bulk-service (same distroless multi-stage pattern as hsm-core-service) + full helm/hsm-bulk-service/ chart (Deployment, Service, ConfigMap, Secret, ServiceAccount/Workload Identity, HPA, PDB, NetworkPolicy, corporate-CA initContainer). Two deliberate differences from hsm-core-service's chart: replicaCount 1 + PDB off by default (scoped bulk-window service, not always-on traffic -- a minAvailable:1 PDB on a single replica blocks all voluntary node drains), and probes.type: tcp by default (no health endpoint exists on SVC yet). Adding the two new modules to the parent POM's <modules> list broke the EXISTING Dockerfile.hsm-core-service/Dockerfile.cek-rotation builds (Maven's reactor needs every module's pom.xml present even with -pl/-am) -- both fixed. hsm-bulk-client deliberately stays a plain runnable jar, no Docker/K8s for it -- scripts/build.sh + scripts/run.sh + config-examples/ instead. Validated via helm lint/template (no live Docker daemon or K8s cluster available in this environment to validate a real docker build/deploy)." },
    { "category": "Security Hardening", "item": "hsm-core-service: stop embedding DB credentials in DATABASE_URL", "status": "C", "notes": "hsm-core-service previously expected credentials embedded directly in DATABASE_URL (postgresql://user:pass@host/db) -- but Hikari and Hibernate both print the JDBC URL at startup unconditionally ('Database JDBC URL [...]', 'HikariPool-1 - Added connection ... url=...'), which ships to Splunk via this service's own log pipeline; an embedded password would land in plaintext in every pod's logs. Fixed by adding separate DATABASE_USERNAME/DATABASE_PASSWORD properties (matching hsm-bulk-service's and application-demo.yml's existing pattern), updated helm/hsm-core-service's Secret template accordingly. Verified: hsm-core-service still starts and passes its health check with the new fields; 55/55 tests still pass." }
  ]
}
