<?xml version="1.0" encoding="UTF-8"?>
<coordinator-app name="${bunitProcess}-coord" frequency="${runfrequency}" start="${startTime}" end="${endTime}" timezone="${timeZone}" xmlns="uri:oozie:coordinator:0.4" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

   <controls>
        <timeout>${timeoutValue}</timeout>
        <concurrency>${concurrencyLevel}</concurrency>
        <execution>${executionOrder}</execution>
        <throttle>${materializationThrottle}</throttle>
   </controls>

   <action>
      <workflow>
        <app-path>${bunitProcessPath}</app-path>
        <configuration>
            <property>
                <name>created_date</name>
                <value>${coord:formatTime(coord:dateOffset(coord:nominalTime(), -1, 'DAY'), "yyyy-MM-dd")}</value>
            </property>
        </configuration>
      </workflow>
   </action>
</coordinator-app>